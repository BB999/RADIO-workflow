name: Input Processing with Gemini CLI
on:
  workflow_call:
    inputs:
      development-report:
        required: true
        type: string
      topic-focus:
        required: false
        type: string
    outputs:
      processed-summary:
        value: ${{ jobs.process.outputs.summary }}
      structured-content:
        value: ${{ jobs.process.outputs.content }}
      metadata:
        value: ${{ jobs.process.outputs.metadata }}

jobs:
  process:
    runs-on: ubuntu-latest
    outputs:
      summary: ${{ steps.gemini.outputs.summary }}
      content: ${{ steps.gemini.outputs.structured-content }}
      metadata: ${{ steps.gemini.outputs.metadata }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Setup Gemini API
        run: |
          echo "📦 Gemini API の準備"
          # Gemini CLIは存在しないため、直接APIを使用
          echo "✅ Gemini API を直接使用します（追加パッケージ不要）"
      
      - name: Process large input with Gemini API
        id: gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API }}
        run: |
          echo "::group::🧠 大容量入力処理開始 with Gemini"
          echo "開始時刻: $(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"
          
          # 入力サイズの確認
          INPUT_LENGTH=$(echo "${{ inputs.development-report }}" | wc -c)
          echo "📏 入力文字数: $INPUT_LENGTH 文字"
          
          # Gemini API キーの確認
          if [ -z "$GEMINI_API_KEY" ]; then
            echo "::error::GEMINI_API_KEY が設定されていません"
            exit 1
          fi
          echo "✅ Gemini API キーが設定されています"
          
          # プロンプトの構築（echoコマンドで作成）
          DEVELOPMENT_REPORT="${{ inputs.development-report }}"
          TOPIC_FOCUS="${{ inputs.topic-focus }}"
          
          {
            echo "あなたは日本のラジオ番組の構成作家です。以下の大容量の開発進捗レポートを分析し、4分間のラジオ番組に適した内容に構造化してください。"
            echo ""
            echo "【入力情報】"
            echo "開発進捗: $DEVELOPMENT_REPORT"
            echo "強調ポイント: ${TOPIC_FOCUS:-なし}"
            echo ""
            echo "【出力要件】"
            echo "以下のJSON形式で出力してください："
            echo ""
            echo '```json'
            echo '{'
            echo '  "summary": "全体の要約（200文字以内）",'
            echo '  "main_highlights": ['
            echo '    "重要な進捗1（50文字以内）",'
            echo '    "重要な進捗2（50文字以内）",'
            echo '    "重要な進捗3（50文字以内）"'
            echo '  ],'
            echo '  "technical_focus": "技術的なハイライト（100文字以内）",'
            echo '  "user_impact": "ユーザーへの影響（100文字以内）",'
            echo '  "next_preview": "次回予告できる内容（80文字以内）",'
            echo '  "radio_hooks": ['
            echo '    "リスナーの関心を引く要素1",'
            echo '    "リスナーの関心を引く要素2"'
            echo '  ],'
            echo '  "tone": "bright|serious|exciting|informative",'
            echo '  "estimated_talk_time": "秒数（数字のみ）"'
            echo '}'
            echo '```'
            echo ""
            echo "【重要な指示】"
            echo "1. ラジオリスナーが理解しやすい平易な日本語を使用"
            echo "2. 技術的すぎる内容は一般向けに翻訳"
            echo "3. 4分間の番組に収まる情報量に調整"
            echo "4. 聞き手の興味を引く「フック」を必ず含める"
            echo "5. JSON形式以外は出力しない"
            echo ""
            echo "処理してください。"
          } > input-processing-prompt.txt

          echo "🚀 Gemini API で大容量入力を処理中..."
          echo "📝 プロンプト長: $(cat input-processing-prompt.txt | wc -c) 文字"
          
          # Gemini API リトライループ (最大3回)
          SUCCESS=false
          for RETRY_COUNT in {1..3}; do
            echo "🔄 Gemini API 呼び出し試行 $RETRY_COUNT/3"
            
            # Gemini CLI実行
            GEMINI_RESULT=$(curl -s -X POST \
              "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-exp:generateContent?key=$GEMINI_API_KEY" \
              -H "Content-Type: application/json" \
              -d "{
                \"contents\": [{
                  \"parts\": [{
                    \"text\": \"$(cat input-processing-prompt.txt | sed 's/"/\\"/g' | tr -d '\n')\"
                  }]
                }],
                \"generationConfig\": {
                  \"temperature\": 0.7,
                  \"maxOutputTokens\": 2048
                }
              }" 2>&1)
            
            # エラーチェック
            if [ $? -eq 0 ] && [ -n "$GEMINI_RESULT" ]; then
              # レスポンスからテキストを抽出
              PROCESSED_CONTENT=$(echo "$GEMINI_RESULT" | jq -r '.candidates[0].content.parts[0].text' 2>/dev/null || echo "")
              
              if [ -n "$PROCESSED_CONTENT" ] && [ "$PROCESSED_CONTENT" != "null" ]; then
                echo "✅ Gemini API 成功 (試行 $RETRY_COUNT/3)"
                SUCCESS=true
                break
              else
                echo "::warning::⚠️ Gemini API から空の応答 (試行 $RETRY_COUNT/3)"
              fi
            else
              echo "::warning::⚠️ Gemini API 呼び出し失敗 (試行 $RETRY_COUNT/3)"
              echo "エラー詳細: $GEMINI_RESULT" | head -20
            fi
            
            # リトライ前の待機
            if [ $RETRY_COUNT -lt 3 ]; then
              echo "⏳ 10秒後に再試行します..."
              sleep 10
            fi
          done
          
          # 最終的な成功チェック
          if [ "$SUCCESS" != "true" ]; then
            echo "::error::❌ Gemini API が3回の試行後も失敗しました"
            echo "::error::最後のエラー詳細:"
            echo "$GEMINI_RESULT" | head -50
            exit 1
          fi
          
          echo "🎯 Gemini 処理結果（最初の1000文字）:"
          echo "$PROCESSED_CONTENT" | head -c 1000
          echo ""
          
          echo "✅ 構造化されたコンテンツを取得"
          echo "📄 処理結果の長さ: $(echo "$PROCESSED_CONTENT" | wc -c) 文字"
          
          # JSONの妥当性チェック
          if echo "$PROCESSED_CONTENT" | jq . > /dev/null 2>&1; then
            echo "✅ 有効なJSON形式を確認"
            
            # 各フィールドを抽出
            SUMMARY=$(echo "$PROCESSED_CONTENT" | jq -r '.summary // "要約の生成に失敗"')
            MAIN_HIGHLIGHTS=$(echo "$PROCESSED_CONTENT" | jq -r '.main_highlights // []' | jq -c .)
            TECHNICAL_FOCUS=$(echo "$PROCESSED_CONTENT" | jq -r '.technical_focus // "技術情報なし"')
            USER_IMPACT=$(echo "$PROCESSED_CONTENT" | jq -r '.user_impact // "影響情報なし"')
            NEXT_PREVIEW=$(echo "$PROCESSED_CONTENT" | jq -r '.next_preview // "次回予告なし"')
            RADIO_HOOKS=$(echo "$PROCESSED_CONTENT" | jq -r '.radio_hooks // []' | jq -c .)
            TONE=$(echo "$PROCESSED_CONTENT" | jq -r '.tone // "informative"')
            TALK_TIME=$(echo "$PROCESSED_CONTENT" | jq -r '.estimated_talk_time // "60"')
            
            echo "📊 抽出した情報:"
            echo "  要約: $SUMMARY"
            echo "  トーン: $TONE"
            echo "  推定時間: ${TALK_TIME}秒"
            
          else
            echo "::error::❌ Gemini APIから有効なJSON形式の応答を受信できませんでした"
            echo "::error::応答内容の先頭500文字:"
            echo "$PROCESSED_CONTENT" | head -c 500
            exit 1
          fi
          
          # 結果をファイルに保存（echoコマンドで構築）
          {
            echo '{'
            echo "  \"summary\": \"$SUMMARY\","
            echo "  \"main_highlights\": $MAIN_HIGHLIGHTS,"
            echo "  \"technical_focus\": \"$TECHNICAL_FOCUS\","
            echo "  \"user_impact\": \"$USER_IMPACT\","
            echo "  \"next_preview\": \"$NEXT_PREVIEW\","
            echo "  \"radio_hooks\": $RADIO_HOOKS,"
            echo "  \"tone\": \"$TONE\","
            echo "  \"estimated_talk_time\": \"$TALK_TIME\","
            echo '  "processing_info": {'
            echo "    \"original_length\": $INPUT_LENGTH,"
            echo "    \"processed_at\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\","
            echo '    "processing_model": "gemini-2.0-flash-exp"'
            echo '  }'
            echo '}'
          } > processed-input.json
          
          # 台本生成用の統合サマリーを作成
          HIGHLIGHTS_LIST=$(echo "$MAIN_HIGHLIGHTS" | jq -r '.[]' | sed 's/^/- /')
          RADIO_SUMMARY=$(printf "【要約】%s\n\n【主要進捗】\n%s\n\n【技術ハイライト】%s\n\n【ユーザーへの影響】%s\n\n【次回予告】%s\n\n【推奨トーン】%s（推定%s秒）" \
            "$SUMMARY" "$HIGHLIGHTS_LIST" "$TECHNICAL_FOCUS" "$USER_IMPACT" "$NEXT_PREVIEW" "$TONE" "$TALK_TIME")
          
          # GitHub Outputsに設定（改行を含む場合の安全な方法）
          {
            echo "summary<<GITHUB_OUTPUT_EOF"
            echo "$RADIO_SUMMARY"
            echo "GITHUB_OUTPUT_EOF"
          } >> $GITHUB_OUTPUT
          
          {
            echo "structured-content<<GITHUB_OUTPUT_EOF"
            cat processed-input.json
            echo "GITHUB_OUTPUT_EOF"
          } >> $GITHUB_OUTPUT
          
          echo "metadata={\"original_length\": $INPUT_LENGTH, \"processed_length\": $(echo \"$RADIO_SUMMARY\" | wc -c), \"model\": \"gemini-2.0-flash-exp\"}" >> $GITHUB_OUTPUT
          
          echo "::endgroup::"
          echo "✅ 入力処理完了！"
      
      - name: Upload processed content artifact
        uses: actions/upload-artifact@v4
        with:
          name: processed-input
          path: |
            processed-input.json
            input-processing-prompt.txt
          retention-days: 7